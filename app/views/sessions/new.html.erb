<h1>Log In</h1>

<% if flash[:alert] %>
  <div class='field_with_errors'><%= flash[:alert] %></div>
<% else %>
  <p class="important">Reminder: Use only the numbers of your CFI</p>
<% end %>

<%= render partial: 'login' %>