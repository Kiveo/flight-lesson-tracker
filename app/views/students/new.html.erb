<h1>Student Sign Up</h1>

<% if flash[:alert] %>
  <div class='field_with_errors'><%= flash[:alert] %></div>
<% else %>
  <p class="important">Note: Student ID will be issued by administration.</p>
<% end %>

<%= render partial: 'form', locals: {student: @student, submit_name: 'Register Student'} %>
